---
paths:
  - "src/gameplay/**"
---

# 游戏性代码规则

- 所有数值必须数据驱动——不允许在代码中硬编码生命值、伤害、速度等数值
- 使用 delta time 确保所有动画和移动与帧率解耦
- 游戏性代码禁止直接引用 UI 模块——通过事件或委托通信
- 实体必须通过接口与子系统通信，禁止直接依赖具体类
- 所有游戏性状态变化必须记录日志（用于回放和 QA）
- 配置类数据必须支持热重载，无需重启游戏
- 随机数必须使用统一的随机数生成器，支持种子设置
