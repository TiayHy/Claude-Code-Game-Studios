---
paths:
  - "assets/**"
---

# 数据文件规则

- 所有数据文件必须使用 JSON Schema 验证
- 数值字段必须标注单位（米、秒、百分比等）
- 枚举类字段必须列出所有可选值
- 支持数据版本控制，允许降级加载旧版本
- 关键数据变更必须通过迁移脚本，而非手动编辑
- 数据文件必须有对应的文档说明每个字段的含义
- 禁止数据文件中包含逻辑代码——纯数据，不允许函数或脚本
