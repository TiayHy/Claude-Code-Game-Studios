---
paths:
  - "src/ai/**"
---

# AI 代码规则

- AI 更新预算：每帧最多 2ms——需通过性能分析验证
- 所有 AI 参数必须可从数据文件调整（行为树权重、感知范围、计时器）
- AI 必须可调试：为所有 AI 状态实现可视化钩子（路径、感知锥、决策树）
- AI 应提前暴露意图——让玩家有时间读取和反应
- 优先使用基于效用系统或行为树的方法，而非硬编码的 if/else 链
- 群体 AI 必须支持阵型、侧翼包抄和角色分配，且从数据驱动
- 所有 AI 状态机必须记录状态转换以便调试
- 永远不要未经验证就信任网络传来的 AI 输入
